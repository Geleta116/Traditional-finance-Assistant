import { DynamicModule, ModuleMetadata, Type } from '@nestjs/common';
import { Locker } from './interfaces';
export interface AsyncModuleConfig extends Pick<ModuleMetadata, 'imports' | 'exports'> {
    useClass?: Type<Locker>;
}
export declare class ScheduleModule {
    static forRoot(options?: AsyncModuleConfig): DynamicModule;
    private static createLockerProvider;
}
