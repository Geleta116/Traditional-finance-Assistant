import { Locker } from './interfaces/locker.interface';
export declare class ScheduleWrapper {
    immediately(name: string, target: Promise<Function>): Promise<Function>;
    retryable(target: Promise<Function>, retries?: number, retry?: number): Promise<Function>;
    lockable(target: Promise<Function>, name: string, locker?: Locker): Promise<Function>;
}
