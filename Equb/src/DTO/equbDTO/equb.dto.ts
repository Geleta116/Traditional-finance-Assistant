export class EqubDTO{
    name: string;
    amount: number
    duration: number // monthly or weakly
    countdown: number // after how many day will we it begin
    description: string
    minMembers: number
}
