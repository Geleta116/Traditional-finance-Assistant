export type CreateUserParams = {
    username: string;
    password: string;
  };

  export type CreateEqub = {
    name : string;
    amount: number;
    creator: string;
    code: string;
    expiredate: Date;
  }